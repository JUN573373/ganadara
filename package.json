{
  "name": "balju-check-web",
  "private": true,
  "version": "0.1.0",
  "description": "발주 수집과 발주체크 엑셀 작성을 위한 정적 웹 도구",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run build && playwright test"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "dependencies": {
    "cheerio": "^1.2.0",
    "exceljs": "4.4.0",
    "tough-cookie": "^6.0.2"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "vite": "8.3.0"
  }
}
